<%= page_heading "Process Inbox #{@inbox.name}" %>

<%= render 'item_develop', item: @item, step: @step %>

<div class="nav_links">
  <%= link_to 'Back to Inbox', inbox_path(@inbox)  %> |
  <%= link_to 'Back to Inboxes', inboxes_path  %>
</div>
