<%= page_heading "Filter Results" %>

<%= render 'task_table', tasks: @tasks, proj_name: true, loc_name: false, options: false, time_en: true, selecting: false %>

<div class="nav_links">
  <button class="button <%=@mode %>" type="button"><%= link_to "New Filter", filter_path %></button>
  <button class="button <%=@mode %>" type="button"><%= link_to "Mass Assign Tags", mass_assign_path(task_ids: @ids, from: "filter") %></button>
</div>
