<p id="notice"><%= notice %></p>

<%= page_heading "#{@item.name}" %>

<div class="nav_links">
  <%= link_to 'Edit Item', edit_inbox_item_path(@inbox,@item) %>
</div>

<div class="item_show_page">
  <p>
    <strong>Description:</strong>
    <%= @item.description %>
  </p>

  <p>
    <strong>Inbox:</strong>
    <%= @item.inbox.name %>
  </p>
</div>

<div class="nav_links">
  <%= link_to 'Add Another Item', new_inbox_item_path(@inbox) %> |
  <%= link_to 'Back to Inbox', @inbox %> | <%= link_to 'Back to Inboxes', inboxes_path %>
</div>
